# 实施计划

## 目标

实现一个简化的机器人迎宾应用：接收人员、对话、会议和时间事件；根据业务规则输出机器人动作或语音效果；通过自动化测试验证核心行为。

## 时间安排

1. 0–15 分钟：阅读题目、创建分支、编写本计划并创建 Draft PR。
2. 15–35 分钟：定义数据模型、状态结构和事件处理接口。
3. 35–70 分钟：实现 `RobotApplication` 的业务规则。
4. 70–95 分钟：编写并完善 pytest 测试。
5. 95–110 分钟：编写架构说明和 ROS 2 判断报告。
6. 110–120 分钟：运行测试、检查提交内容、推送并将 PR 转为 Ready for review。

## 模块划分

- `robot_application/models.py`
  - 定义不可变的 `Event` 和 `Effect` 数据模型。
- `robot_application/application.py`
  - 保存应用状态。
  - 处理输入事件。
  - 返回效果列表，不直接调用 ROS 2 或机器人硬件 SDK。
- `tests/`
  - 使用 pytest 验证业务规则和状态隔离。
- `ARCHITECTURE.md`
  - 说明模块边界、状态所有权和未来扩展方式。
- `REPORT.md`
  - 记录测试结果、未完成项和 ROS 2 日志判断。
- `AI_USAGE.md`
  - 如实记录 AI 的使用范围及人工验证方式。

## 状态设计

`RobotApplication` 负责保存私有状态：

- 当前对话是否进行中。
- 当前会议是否进行中。
- 每个 `person_id` 的本次到访状态：
  - 是否在场；
  - 是否已经欢迎；
  - 离开时间；
  - 是否已经送客。

状态不使用全局可变变量。`snapshot()` 返回状态副本，避免调用方修改内部状态。

## 业务规则实现顺序

1. 实现 `PERSON_ENTERED`：
   - 首次进入且没有对话、会议时，生成挥手和欢迎语效果。
   - 重复进入不生成重复欢迎。
2. 实现 `CONVERSATION_STARTED`、`CONVERSATION_ENDED`、`MEETING_STARTED`、`MEETING_ENDED`：
   - 更新应用级对话、会议状态。
3. 实现 `PERSON_LEFT`：
   - 记录离开时间，不立即送客。
4. 实现 `TICK`：
   - 找出离开满 10 秒且尚未送客的人员。
   - 每人仅生成一次送客效果。
5. 实现离开后返回：
   - 若 10 秒内返回，取消待送客状态，不再次欢迎。
   - 若已完成送客后再次进入，视为新的到访并再次欢迎。

## 测试计划

至少覆盖以下场景：

1. 首次进入生成 `wave_hand` 和欢迎语。
2. 同一人员重复进入不会重复欢迎。
3. 对话期间进入不会欢迎。
4. 会议期间进入不会欢迎。
5. 离开后不足 10 秒的 `TICK` 不送客。
6. 离开满 10 秒的 `TICK` 送客一次。
7. 后续 `TICK` 不重复送客。
8. 人员在 10 秒内返回时不送客，也不再次欢迎。
9. 人员已送客后再次进入时重新欢迎。
10. 修改 `snapshot()` 返回值不会影响内部状态。

## 验证方式

- 运行 `pytest -q`，确保全部测试通过。
- 人工检查业务规则与题目逐条一致。
- 检查 `snapshot()` 不泄露可修改的内部对象。
- 根据提供的 ROS 2 日志，将“已提交请求”“已异步接受”“真实动作完成”明确区分。

## 风险与处理

- 时间戳边界：以“离开后满 10 秒”为送客条件，测试 `9.9` 秒、`10.0` 秒等边界。
- 重复事件：为人员状态增加已欢迎、已送客标记，保证幂等。
- 状态混乱：将事件分发与具体事件处理拆分为私有方法，保持逻辑清晰。
- ROS 2 链路不可用：业务模块保持独立，通过 `REPORT.md` 如实记录验证结论和下一步排查动作。

